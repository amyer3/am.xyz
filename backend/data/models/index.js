const { user_fields, user_opts } = require('./user.model')
const { post_fields, post_opts } = require('./post.model')


module.exports = {
    user_fields,
    user_opts,
    post_fields,
    post_opts
}